ldap_servers.settings:
  type: mapping
  label: Settings
  mapping:
    encryption:
      type: boolean
      label: 'Require HTTPS on credential pages'
    encrypt_key:
      type: boolean
    require_ssl_for_credentials:
      type: integer
